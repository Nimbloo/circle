# Plano — Épico #25, leva 2 (restos, SLAs/automações, busca, organização, import/API/webhooks, roadmap, triage IA)

**Spec:** `docs/superpowers/specs/2026-09-04-epico-25-leva-2-design.md`

## Estado (handoff entre agentes)

> Atualize este bloco ao fechar cada task e antes de pausar. Quem retoma (Codex ou
> Claude) lê daqui, não da memória de sessão.

- **Onde:** worktree `C:/Projetos/circle-functional-audit`, branch `danilo/epico-25-leva-2`
  (base `develop` = v0.28.0 + docs). Grupos 1–6 em worktrees isolados, mergeados aqui.
- **Feito:** spec e plano (Claude, 2026-09-04). Nenhum grupo concluído ainda.
- **Última verificação:** —
- **Próximo passo:** relançar os seis grupos (prompts na spec, uma seção por grupo) a partir
  desta branch; depois integração (migrations `0043+`), verificação, smoke, PR
  (Closes #94 #97 #99 #100 #101 #102), release MINOR.
- **Bloqueios / decisões pendentes:** (1) Bedrock em produção segue bloqueado (formulário de
  use case) — as features com IA sobem com fallback honesto. (2) **Limite de uso da conta
  interrompeu as duas primeiras tentativas** (04/09, resets 21:50): os grupos morreram antes
  de commitar. Trabalho parcial salvo como patch em
  `%TEMP%/claude/C--Projetos-circle/<sessão>/scratchpad/leva2-parcial/` (migration de
  hierarquia/desativação, migration de vetores de busca, ajuste do `task-item-view`); pode
  ser descartado e refeito, é pequeno.

## Task 1 — Restos + SLAs e automações (#97)

- [ ] `checked` regravado no item vinculado; `deleteIssue` limpa S3; vídeo com popover.
- [ ] `team_sla` + aplicação de dueDate + indicadores/filtro; `team_automation` + motor +
      UI em Workflows & automations; audit.

## Task 2 — Busca (#99)

- [ ] `search_vector` + GIN; `GET /api/v1/search` com ranking/snippet/fallback; semântica
      opcional; palette e busca dedicada agrupadas com chips e Save search.

## Task 3 — Organização (#100)

- [ ] `team.parent_id`, `initiative.parent_id` (rollup), `app_user.deactivated_at`,
      escopo de Guest com teste por rota.

## Task 4 — Import/export, API pública, webhooks (#101)

- [ ] Import com preview/commit idempotente (CSV/Linear/Jira); export JSON; `api_token` +
      `/api/public/v1` + OpenAPI; `webhook` + deliveries assinadas com retry lazy; UI em
      Settings.

## Task 5 — Roadmap (#102)

- [ ] Tela Roadmap por initiative; `project_dependency` com seta e alerta;
      `project_snapshot` lazy + gráfico.

## Task 6 — Triage com IA (#94)

- [ ] `issue_triage_suggestion`; geração assíncrona/lazy com fallback heurístico; card
      Accept/Edit/Dismiss.

## Task 7 — Integração e entrega

- [ ] Merge dos seis, migrations consolidadas, typecheck/lint/test/build, smoke.
- [ ] `docs/PENDENCIAS.md`, PR para `develop`, release, verificação em produção.
