# Plano — Épico #25, leva 2 (restos, SLAs/automações, busca, organização, import/API/webhooks, roadmap, triage IA)

**Spec:** `docs/superpowers/specs/2026-09-04-epico-25-leva-2-design.md`

## Estado (handoff entre agentes)

> Atualize este bloco ao fechar cada task e antes de pausar. Quem retoma (Codex ou
> Claude) lê daqui, não da memória de sessão.

- **Onde:** worktree `C:/Projetos/circle-functional-audit`, branch `danilo/epico-25-leva-2`
  (base `develop` = v0.28.0 + docs). Grupos 1–6 em worktrees isolados, mergeados aqui.
- **Feito:** Tasks 1–6 (seis grupos mergeados em `danilo/epico-25-leva-2`; migrations
  consolidadas em `0043_epico_25_leva_2`, `0044_search_vectors` e
  `0045_leva_2_import_webhooks_triage_roadmap`).
- **Última verificação:** typecheck, lint, 142 arquivos / 909 testes e build (Claude,
  2026-09-04). Smoke no dev server: roadmap por initiative, dependência criada e ciclo
  rejeitado com 400, snapshot do dia, busca agrupada no palette com destaque, triagem
  heurística achando duplicata de 80% com card na fila, SLA de 4 h salvo, automação
  aplicando label sozinha na issue criada em triage, token público autenticando (401 sem
  token) e telas de Workflows/Webhooks/API tokens.
- **Próximo passo:** Task 7 — PR para `develop` (Closes #94 #97 #99 #100 #101 #102), CI,
  merge, `chore: release v0.29.0`, PR para `main`, verificação em produção, docs.
- **Bloqueios / decisões pendentes:** Bedrock segue bloqueado em produção (formulário de use
  case); triagem e busca semântica sobem com fallback honesto e passam a usar a IA assim que a
  conta for liberada.

## Task 1 — Restos + SLAs e automações (#97)

- [x] `checked` regravado no item vinculado; `deleteIssue` limpa S3; vídeo com popover.
- [x] `team_sla` + aplicação de dueDate + indicadores/filtro; `team_automation` + motor +
      UI em Workflows & automations; audit.

## Task 2 — Busca (#99)

- [x] `search_vector` + GIN; `GET /api/v1/search` com ranking/snippet/fallback; semântica
      opcional; palette e busca dedicada agrupadas com chips e Save search.

## Task 3 — Organização (#100)

- [x] `team.parent_id`, `initiative.parent_id` (rollup), `app_user.deactivated_at`,
      escopo de Guest com teste por rota.

## Task 4 — Import/export, API pública, webhooks (#101)

- [x] Import com preview/commit idempotente (CSV/Linear/Jira); export JSON; `api_token` +
      `/api/public/v1` + OpenAPI; `webhook` + deliveries assinadas com retry lazy; UI em
      Settings.

## Task 5 — Roadmap (#102)

- [x] Tela Roadmap por initiative; `project_dependency` com seta e alerta;
      `project_snapshot` lazy + gráfico.

## Task 6 — Triage com IA (#94)

- [x] `issue_triage_suggestion`; geração assíncrona/lazy com fallback heurístico; card
      Accept/Edit/Dismiss.

## Task 7 — Integração e entrega

- [x] Merge dos seis, migrations consolidadas, typecheck/lint/test/build, smoke.
- [ ] `docs/PENDENCIAS.md`, PR para `develop`, release, verificação em produção.
