# Débito de funcionalidade, experiência e dívida pequena — Plano

## Estado (handoff entre agentes)

- **Onde:** branch `danilo/debito-ux` (a partir da `develop` @ `6d8b36b`). Frentes B, C e L
  foram feitas em worktrees de subagente e integradas por merge.
- **Feito:** todas as frentes A–L (L = preferências mortas, achada durante a A). Extras
  achados no caminho e corrigidos: `displayNames` (regressão de default), painel "Diffs"
  mostrando hash, emoticons nos comentários, grupos da lista colapsáveis, swimlanes com
  `content-visibility`, "Hidden columns" ao lado das swimlanes, teste instável de rank.
- **Última verificação:** ver o PR (suíte completa, typecheck, lint e build no branch integrado).
- **Próximo passo:** concluído — v0.43.0 em prd.
- **Bloqueios:** nenhum.

## Decisões do usuário (2026-09-23)

- **Stubs "Soon"** que dependem de algo que não existe: **remover da UI**.
- **Sub-agrupamento** (Rows/Sub-grouping) e **"Create document"** no Resources do projeto:
  **implementar** como funcionalidade real.
- **Estrutural** (cliente baixa todas as issues; bundle): **medir e decidir com número**.
  Bundle-analyzer + medição; a refatoração do store vira plano escrito para aprovação.

## Frentes

- [x] **A. Remover stubs "Soon":** notificações Desktop/Mobile, "Open in desktop app", signing
      key, "Configure coding tools", Skills, MCP connectors, "Browse integrations", Slack no Agent.
- [x] **B. Sub-agrupamento:** lista (sub-grouping dentro do grupo) e board (rows/swimlanes),
      persistido no display settings, com o select hoje desabilitado funcionando.
- [x] **C. Create document no Resources do projeto:** cria documento do time do projeto e
      o vincula como recurso.
- [x] **D. `list-exit`:** linha que sai da lista (adiada, excluída, arquivada) anima a saída.
- [x] **E. Diálogos sem latch:** remover automação (workflow do time). O de webhook não lê o
      alvo no texto — não pisca; a guarda passou a cobrir descrição e ternário.
- [x] **F. `SettingsCard`** em audit log, integrations, templates (issue e projeto), project
      statuses e pulse; raio do popover e delay do tooltip do roadmap alinhados ao sistema.
- [x] **G. Triagem:** título do header acompanha a aba ativa.
- [x] **H. `trim()`** na API pública de issues (create/edit), automations do time e `/me`.
- [x] **I. Testes sem `sleep` fixo** (reviews-layout, triage-queue, roadmap-ui, issue-detail-lifecycle).
- [x] **J. `search_vector`/GIN:** NÃO declarados (o drizzle-kit geraria ADD COLUMN
      duplicado); documentados no `schema.ts`.
- [x] **K. Medição:** bundle-analyzer, payload real das issues; plano da refatoração do store
      (`specs/2026-09-23-issues-cache-e-bundle-medicao.md`).
- [x] **L. Preferências mortas:** 12 passam a ter efeito, 8 saem da UI e do store; guarda
      `preferences-consumed-guard` impede controle que não faz nada.
- [ ] Verificação: `pnpm test`, `typecheck`, `lint`, `build`; PR → develop; revisão do CodeRabbit
      no PR de release; release; verificação em produção.
