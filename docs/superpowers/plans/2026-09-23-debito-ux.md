# Débito de funcionalidade, experiência e dívida pequena — Plano

## Estado (handoff entre agentes)

- **Onde:** branch `danilo/debito-ux` (a partir da `develop` @ `6d8b36b`, v0.42.0 em produção).
  Frentes B e C rodam em worktrees de subagente a partir do mesmo commit.
- **Feito:** plano escrito.
- **Última verificação:** —
- **Próximo passo:** executar as frentes A–K.
- **Bloqueios:** nenhum.

## Decisões do usuário (2026-09-23)

- **Stubs "Soon"** que dependem de algo que não existe: **remover da UI**.
- **Sub-agrupamento** (Rows/Sub-grouping) e **"Create document"** no Resources do projeto:
  **implementar** como funcionalidade real.
- **Estrutural** (cliente baixa todas as issues; bundle): **medir e decidir com número**.
  Bundle-analyzer + medição; a refatoração do store vira plano escrito para aprovação.

## Frentes

- [ ] **A. Remover stubs "Soon":** notificações Desktop/Mobile, "Open in desktop app", signing
      key, "Configure coding tools", Skills, MCP connectors, "Browse integrations", Slack no Agent.
- [ ] **B. Sub-agrupamento:** lista (sub-grouping dentro do grupo) e board (rows/swimlanes),
      persistido no display settings, com o select hoje desabilitado funcionando.
- [ ] **C. Create document no Resources do projeto:** cria documento do time do projeto e
      o vincula como recurso.
- [ ] **D. `list-exit`:** linha que sai da lista (adiada, excluída, arquivada) anima a saída.
- [ ] **E. Diálogos sem latch:** remover status (workflow do time) e remover webhook.
- [ ] **F. `SettingsCard`** em audit log, integrations, templates (issue e projeto), project
      statuses e pulse; raio do popover e delay do tooltip do roadmap alinhados ao sistema.
- [ ] **G. Triagem:** título do header acompanha a aba ativa.
- [ ] **H. `trim()`** na API pública de issues (create/edit), automations do time e `/me`.
- [ ] **I. Testes sem `sleep` fixo** (reviews-layout, triage-queue, roadmap-ui, issue-detail-lifecycle).
- [ ] **J. `search_vector`/GIN declarados no `schema.ts`** sem gerar migration.
- [ ] **K. Medição:** bundle-analyzer, payload real das issues; plano da refatoração do store.
- [ ] Verificação: `pnpm test`, `typecheck`, `lint`, `build`; PR → develop; revisão do CodeRabbit
      no PR de release; release; verificação em produção.
